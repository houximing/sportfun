{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Stadium creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('stadium') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
