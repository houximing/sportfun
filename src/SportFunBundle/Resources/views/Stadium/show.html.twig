{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Stadium</h1>
     {{ form(delete_form) }}
    <table class="record_properties table table-hover table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entity.code }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.typetext }}</td>
            </tr>
            <tr>
                <th>Abn</th>
                <td>{{ entity.abn }}</td>
            </tr>
            <tr>
                <th>Contactperson</th>
                <td>{{ entity.contactPerson }}</td>
            </tr>
            <tr>
                <th>Contactnumber</th>
                <td>{{ entity.contactNumber }}</td>
            </tr>
            <tr>
                <th>Contactemail</th>
                <td>{{ entity.contactEmail }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ entity.logo }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ entity.address }}</td>
            </tr>
            <tr>
                <th>Suburb</th>
                <td>{{ entity.suburb }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ entity.state }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ entity.postcode }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ entity.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ entity.longitude }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ entity.statustext }}</td>
            </tr>
            <tr>
                <th>Chain</th>
                <td>
                    <span class="glyphicon {{ entity.chainicon }}" aria-hidden="true"></span>
                </td>
            </tr>
        </tbody>
    </table>

        <a href="{{ path('stadium') }}" class="btn btn-warning">
            Back to the list
        </a>

        <a href="{{ path('stadium_edit', { 'id': entity.id }) }}" class="btn btn-info">
            Edit
        </a>

</ul>
{% endblock %}
